---
# yaml-language-server: $schema=https://schema.blue-build.org/recipe-v1.json
modules:
  - type: dnf
    repos:
      cleanup: true
      copr:
        - crashdummy/Displaylink
    install:
      packages:
        - displaylink

  - type: initramfs
