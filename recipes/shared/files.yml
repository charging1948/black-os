---
# yaml-language-server: $schema=https://schema.blue-build.org/recipe-v1.json

modules:
  - type: files
    files:
      - source: shared
        destination: /
  - type: script
    snippets:
      - "mkdir -p /nix"
